version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install -r requirements.txt
run:
  command: gunicorn -w 4 -b 0.0.0.0:8080 app:server
  network:
    secrets:
      - arn:aws:secretsmanager:us-east-1:851725355134:secret:space-exploration-app/environment-config-MZcLKq
